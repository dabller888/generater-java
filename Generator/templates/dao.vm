package qy.stage.model;

/**
* $table.TableName
*/
public class $table.TableName {
#foreach($col in $columns)
    private $col.AsType $col.ColumnName;
#end

#foreach($col in $columns)
    public $col.AsType set${col.ColumnName}($col.AsType){
        this.$col.ColumnName;
    }

    public $col.AsType get${col.ColumnName}()}{
        return $col.ColumnName;
    }

#end
}
